// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	site: 'https://c4554ndr4.github.io',
	base: '/portfolio',
	trailingSlash: 'always',
	devToolbar: { enabled: false },
});
